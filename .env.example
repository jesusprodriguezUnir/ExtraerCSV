# Configuración de Elasticsearch/Kibana
# Copia este archivo a .env y completa con tus credenciales reales

# URL del servidor Elasticsearch (incluir protocolo https://)
# Ejemplo: https://elk.unir.net
ELASTICSEARCH_HOST=https://tu-servidor-elasticsearch.com

# Credenciales de acceso
# Usuario con permisos de lectura en los índices de logs
ELASTICSEARCH_USER=tu_usuario

# Contraseña (NUNCA commitear el archivo .env con la contraseña real)
ELASTICSEARCH_PASSWORD=tu_contraseña_segura

# Patrón de índices a consultar
# Usar * para wildcards, separar múltiples índices con coma
# Ejemplos: logs-*, logs-aplicacion-*, logs-2024.*
ELASTICSEARCH_INDEX=logs-*

# ===== CONFIGURACIÓN OPCIONAL =====

# Verificar certificados SSL (true/false)
# Usar false solo en desarrollo con certificados autofirmados
# ELASTICSEARCH_VERIFY_SSL=true

# Timeout de conexión en segundos (default: 300)
# Aumentar para consultas muy grandes
# ELASTICSEARCH_TIMEOUT=300

# Tamaño de batch para scroll (default: 1000)
# Ajustar según memoria disponible
# ELASTICSEARCH_SCROLL_SIZE=1000

# Tiempo de vida del scroll (default: 5m)
# Aumentar si el procesamiento es lento
# ELASTICSEARCH_SCROLL_TIMEOUT=5m

# ===== PROXY LOCAL (opcional) =====
# Usar el proxy reverso local `proxy_es.py` cuando la VPN bloquea el acceso directo.
# Ejemplo: arrancar proxy y apuntar la app a http://localhost:9200
# PROXY_TARGET_HOST puede apuntar a la URL externa de Elasticsearch/Kibana
PROXY_TARGET_HOST=https://elk.unir.net
PROXY_TARGET_PORT=443
PROXY_USER=dev-academico
PROXY_PASSWORD=tu_contraseña_segura
PROXY_LISTEN_HOST=127.0.0.1
PROXY_LISTEN_PORT=9200
# Si usas variables de entorno de proxy corporativo, considera NO_PROXY para localhost
# NO_PROXY=localhost,127.0.0.1
